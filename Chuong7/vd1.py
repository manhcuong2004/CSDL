#append() thêm vào cuối List
#insert(i,x) chèn x vào vị trí i
#remove(x) xóa phần tử x đầu list
#del(L[i]) xóa khi biết index
#sort() sắp xếp tăng dần # reverse=True
#reverse() đảo ngược thứ tự
#count(x) đếm; clear(L) xóa tất cả trong L
#x=L.pop(i) xóa và lấy
Chương7
#isnumeric()isdecimal() kí tự số và ko có khoảng trắng
#isalpha() chữ và ko khoảng trắng
#isalnum chữ và số ko kt
#isspace() các ký tự trắng, hoặc dấu tab, hoặc dấu ngắt dòng
#istitle() viết hoa chữ đầu mỗi từ
#startswith(str) Trả về True nếu chuỗi bắt đầu bởi chuỗi str
#endswith(str) Trả về True nếu chuỗi kết thúc bởi chuỗi str
#' '.join(ListOfString) chuyển L thành str
#split(str) chuyển str thành L
#rjust(n,ch), ljust(n,ch), center(n,ch) thêm ch vào
#strip(str), rstrip(str), and lstrip(str)xóa các ký tự có trong chuỗi str ở 2 đầu
#find(str,n)
#replace(oldvalue, newvalue, n)
#capitalize() viết hoa chữ đầu